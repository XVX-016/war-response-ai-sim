{
  "name": "war-response-ai-sim",
  "private": true,
  "scripts": {
    "backend": "uvicorn backend.main:app --reload --port 8000",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
